<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ödeme Başarılı</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="~/css/SuccessPage.css">
</head>
<body>
    <style>
        body {
    background: linear-gradient(135deg, #00c853, #4caf50);
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    font-family: 'Arial', sans-serif;
    color: #fff;
}

.success-container {
    background-color: #fff;
    border-radius: 15px;
    padding: 30px;
    max-width: 600px;
    width: 90%;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
    color: #333;
    text-align: center;
}

.success-icon-container {
    background: #e8f5e9;
    border-radius: 50%;
    width: 100px;
    height: 100px;
    display: flex;
    justify-content: center;
    align-items: center;
    margin: 0 auto;
}

.success-icon {
    font-size: 50px;
    color: #4caf50;
}

.success-message {
    font-size: 1.2rem;
    color: #555;
}

.btn-primary {
    background-color: #4caf50;
    border: none;
    border-radius: 25px;
    font-size: 1rem;
    transition: all 0.3s ease;
}

.btn-primary:hover {
    background-color: #388e3c;
    transform: scale(1.05);
}



    </style>
    <div class="success-container text-center d-flex flex-column justify-content-center align-items-center">
        <div class="success-icon-container mb-4">
            <div class="success-icon">
                <i class="bi bi-check-circle-fill"></i>
            </div>
        </div>
        <h2 class="text-success">Ödeme Başarılı!</h2>
        <p class="success-message mt-2 mb-4">@ViewBag.Message</p>
        <button onclick="window.location.href='@Url.Action("Order", "UserGetOrders")'" class="btn btn-primary px-5 py-3">Anasayfaya Dön</button>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
