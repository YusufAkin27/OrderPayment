<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ödeme Başarısız</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="~/css/ErrorPage.css">
</head>
<body>
    <style>
        body {
    background: linear-gradient(135deg, #ff5252, #d50000);
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    font-family: 'Arial', sans-serif;
    color: #fff;
}

.error-container {
    background-color: #fff;
    border-radius: 15px;
    padding: 30px;
    max-width: 600px;
    width: 90%;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
    color: #333;
    text-align: center;
}

.error-icon-container {
    background: #fbe9e7;
    border-radius: 50%;
    width: 100px;
    height: 100px;
    display: flex;
    justify-content: center;
    align-items: center;
    margin: 0 auto;
}

.error-icon {
    font-size: 50px;
    color: #d32f2f;
}

.error-message {
    font-size: 1.2rem;
    color: #555;
}

.btn-warning {
    background-color: #f57c00;
    border: none;
    border-radius: 25px;
    font-size: 1rem;
    transition: all 0.3s ease;
    color: #fff;
}

.btn-warning:hover {
    background-color: #e65100;
    transform: scale(1.05);
}



    </style>
    <div class="error-container text-center d-flex flex-column justify-content-center align-items-center">
        <div class="error-icon-container mb-4">
            <div class="error-icon">
                <i class="bi bi-x-circle-fill"></i>
            </div>
        </div>
        <h2 class="text-danger">Ödeme Başarısız!</h2>
        <p class="error-message mt-2 mb-4">@ViewBag.Message</p>
        <button onclick="window.location.href='@Url.Action("Confirm", "Order")'" class="btn btn-warning px-5 py-3">Tekrar Deneyin</button>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
