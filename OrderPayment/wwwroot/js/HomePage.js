function addToCart(productId) {
    // You could add your logic here to interact with the backend or cart service
    alert("Ürün sepete eklendi! Ürün ID: " + productId);
    // Optionally, you could also add code to visually indicate that the item was added
}
